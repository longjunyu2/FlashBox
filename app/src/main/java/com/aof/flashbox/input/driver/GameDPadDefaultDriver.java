package com.aof.flashbox.input.driver;

import com.aof.flashbox.input.widget.BaseLayerConfig;

public class GameDPadDefaultDriver extends BaseDriver {

    public GameDPadDefaultDriver(BaseLayerConfig config) {
        super(config);
    }
}
