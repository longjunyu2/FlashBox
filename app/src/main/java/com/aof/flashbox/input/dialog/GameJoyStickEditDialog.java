package com.aof.flashbox.input.dialog;

import android.content.Context;

import androidx.annotation.NonNull;

import com.aof.flashbox.input.widget.GameJoystickLayerConfig;

public class GameJoyStickEditDialog extends GameDPadEditDialog {

    public GameJoyStickEditDialog(@NonNull Context context, GameJoystickLayerConfig config) {
        super(context, config);
    }
}
